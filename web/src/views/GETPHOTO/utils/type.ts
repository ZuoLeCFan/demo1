export interface ReturnDataType {
  path: string;
  url: string;
  fileName: string;
}

export interface ShowPageType{
  id: number;
  url: string;
  status: number,
  searchText: string
}

export interface SearchType{
  id: number;
  url: string;
  path: string
  name: string,
  searchText: string
}

